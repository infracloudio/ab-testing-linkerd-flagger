apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: backend-a
  name: backend-a
  namespace: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-a
  strategy: {}
  template:
    metadata:
      labels:
        app: backend-a
    spec:
      containers:
      - image: backend-a
        imagePullPolicy: Never
        name: backend-a
        ports:
        - containerPort: 8080
        resources: {}
        env:
        - name: BACKEND_ENV
          value: "backend-a"
        - name: VERSION
          value: "primary"
        - name: HEADER
          value: "x-backend"
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP

---

apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend-a
  name: backend-a
  namespace: test
spec:
  ports:
  - name: 50051-50051
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: backend-a
  type: ClusterIP